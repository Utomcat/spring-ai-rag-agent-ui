# 开发规范

## 代码风格

- 使用 ES Module 模块化
- 组件采用 `<script setup>` 语法
- TypeScript 类型定义位于 `src/data/` 目录
- API 接口统一封装在 `src/api/` 目录

## 提交规范

建议使用 Conventional Commits 规范：

```
feat: 新增用户管理功能
fix: 修复聊天会话切换问题
docs: 更新项目文档
style: 调整仪表盘样式
refactor: 重构请求拦截器
```
